# 質問は作業の区切り後だけに行う（旧計画・完了記録）

同日の最新指示により、質問UIの使用許可は撤回された。現行方針は通常チャットのみ。作業の区切り・回答待ち条件は維持する。この文書は当時の記録であり、現在の質問ツール使用許可ではない。

## 目的

ユーザーの「AskUserQuestionは必ず作業の区切り後に行ってください」という指示を反映する。先に提示した質問ツールの一律拒否案は取り下げ、利用できる質問UIも通常チャットも同じ区切り・回答待ち条件で扱う。

## 範囲と確認

- 変更: AGENTS、対話資料、Claudeアダプタ、構成検査、計画・履歴、共有learnings。
- 維持: AI目視確認、制作ゲート、既存プロジェクト、個人設定、権限承認。質問拒否フックは追加しない。
- 区切り: 当該作業で起動した処理を完了または安全に停止し、途中結果を保存して、完了事項・未決事項を提示できる状態。
- 質問はメインだけが区切り後に行い、回答まで次の作業を始めない。未応答を承認扱いにしない。ツールが環境・モード上利用できない場合は通常チャットで質問してターンを終了する。
- 区切りごとの不要な質問を義務化しない。既存の承認が有効なら繰り返さない。

## ステップ

- [x] 1. 共通ルールと対話資料を区切り後の質問・回答待ちへ更新する（確認: 一律禁止案と矛盾しない）。
- [x] 2. ClaudeアダプタでAskUserQuestionの使用時点と利用不可時の通常チャットを明記する（確認: 共通層には製品固有名を持ち込まない）。
- [x] 3. 構成検査を新しいタイミング条件へ更新する（確認: 作業中の質問と未回答での再開を推奨する文言がない）。
- [x] 4. 構成検査、hook自己診断、公開Python自己診断、diff検査を実行する（確認: 既存機能がPASS）。
- [x] 5. レビューと履歴を記録する（確認: 質問ツールの実行を必要もなく発生させず、クライアントUIの実動作未検証を明記する）。

## 進捗メモ

- 2026-09-06: ユーザーの直接指示で方針を「一律禁止」から「作業の区切り後だけ使用」へ確定した。先の未承認フック案は取り下げた。
- 前回完了タスクは tasks/history/2026-09-06-dialogue-ai-visual-review.md に保存済み。

## レビュー

- 完了内容: 区切り後だけ質問する規定、回答待ち、利用不可時の通常チャットを共通ルールとClaudeアダプタへ反映。質問の一律拒否フック案は撤回した。
- 確認結果: 構成検査558項目、hook自己診断、公開Python自己診断、git diff --checkがPASS。静的な指示と既存機能を検証し、質問UIを不要に発生させる実動作試験は行っていない。
- 残課題: 本変更内はなし。Codex本体のUI不具合を修正・再現したという意味ではない。コミット・プッシュは未実施。
- 学び: 質問ツールの可否と利用時点を分け、ユーザーが指定した作業の区切り・回答待ち条件を正本とアダプタで揃える。共有learningsへ記録済み。
